<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="itwillbs.p2c3.boogimovie.mapper.ReservationMapper">
	  <select id="selectMovieReservation" resultType="itwillbs.p2c3.boogimovie.vo.MovieVO" parameterType="String">
		SELECT movie_name 
		FROM movie JOIN reservation
		ON movie.movie_num = reservation.movie_num
		JOIN member 
		ON member.member_id = reservation.member_id
	  	WHERE member.member_id = #{id}
	  </select>
	  
	  <select id="selectDateReservation" resultType="itwillbs.p2c3.boogimovie.vo.TicketVO" parameterType="String">
		SELECT screen_date
		FROM member JOIN reservation
		ON member.member_id = reservation.member_id 
		JOIN ticket
		ON ticket.reservation_num = reservation.reservation_num
		WHERE member.member_id = #{id}
	  </select>
	  
  </mapper>
